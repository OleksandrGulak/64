# Passport Auth App

## Опис

Сервер на Express.js з авторизацією через Passport (локальна стратегія, email + пароль) та сесіями.

## Встановлення

npm install express express-session passport passport-local bcrypt dotenv pug
npm start

```
passport-auth-app/
├── routes/
│   ├── auth.js
│   └── protected.js
├── views/
│   └── login.pug
├── models/
│   └── user.js
├── config/
│   └── passport.js
├── app.js
├── server.js
├── package.json
├── .env
└── README.md
```

## 📍 Опис маршрутів
- GET /login
Відображає сторінку входу з формами для логіну та реєстрації. Використовується шаблон PUG.
- POST /login
Обробляє логін користувача через Passport (локальна стратегія). У разі успіху перенаправляє на захищену сторінку /protected, у разі невдачі — повертає на /login.
- POST /register
Реєструє нового користувача. Пароль хешується за допомогою bcrypt. Після реєстрації користувач перенаправляється на сторінку входу.
- GET /logout
Завершує сесію користувача та очищає дані автентифікації. Після виходу перенаправляє на /login.
- GET /protected
Захищений маршрут, доступний лише для авторизованих користувачів. Якщо користувач автентифікований, відображається привітання з email. Якщо ні — перенаправлення на /login.

## Технології
- Node.js
- Express.js
- Passport.js
- express-session
- bcrypt
- PUG
